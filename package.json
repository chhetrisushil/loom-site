{
  "name": "loom-site",
  "version": "0.0.1",
  "private": true,
  "description": "Documentation site for Loom — the execution kernel for agentic applications.",
  "type": "module",
  "packageManager": "pnpm@9.14.4",
  "scripts": {
    "sync": "node scripts/sync-docs.mjs",
    "dev": "pnpm sync && astro dev",
    "build": "pnpm sync && astro build",
    "preview": "astro preview",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^5.1.1",
    "@astrojs/mdx": "^4.0.3",
    "@astrojs/sitemap": "^3.2.1",
    "shiki": "^1.24.4"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
